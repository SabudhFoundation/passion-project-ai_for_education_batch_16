# external requirements
click
Sphinx
coverage
awscli
flake8
python-dotenv>=0.5.1



# ─────────────────────────────────────────────────────────────
# Requirements for:
#   src/backend/scrapers/naukri.com_scraper.py
#   src/backend/loaders/reducto_loader.py
#
# Install with:
#   pip install -r requirements.txt
#
# ALSO install Chrome on Linux (one-time, system-level):
#   wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
#   sudo apt install ./google-chrome-stable_current_amd64.deb -y
# ─────────────────────────────────────────────────────────────

# --- Scraper (naukri_scraper.py) ---
selenium>=4.18.0            # Browser automation for JS-rendered pages
webdriver-manager>=4.0.1    # Auto-downloads matching ChromeDriver binary
beautifulsoup4>=4.12.0      # HTML parsing after Selenium renders the page
lxml>=5.1.0                 # Fast HTML parser backend for BeautifulSoup

# --- Resume Loader (resume_loader.py) ---
reducto>=0.1.0              # PDF extraction via Reducto cloud API

# --- Utilities ---
python-dotenv>=1.0.0        # Load REDUCTO_API_KEY from .env file

langfuse